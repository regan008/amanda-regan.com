baseurl = "https://www.amanda-regan.com/"
title = "Amanda Regan"
languageCode = "en-us"
defaultContentLanguage = "en"

# Pagination
pagination.pagerSize = 3
pagination.path = "page"

# Theme to use
theme = "hugo-bootstrap-5"

[taxonomies]
  tag = "tags"
  category = "categories"

summaryLength = 10

# Google analytics
# googleAnalytics = ""

[languages.en]
  languageName = "English"

  # Replace the default footer message with a localized message
  # copyright = "Hugo template made with ❤ by Xzya"

[[menu.main]]
  url = "/"
  name = "ABOUT"
  weight = 1

[[menu.main]]
  url = "https://raw.githubusercontent.com/regan008/C-V/master/ARegan-cv.pdf"
  name = "CV"
  weight = 2

[[menu.main]]
  url = "/projects"
  name = "RESEARCH & PROJECTS"
  weight = 4

[[menu.main]]
  url = "/courses"
  name = "TEACHING"
  weight = 5

[[menu.main]]
url = "/office-hours"
name = "OFFICE HOURS"
weight = 6






# Links format

[permalinks]
  post = "/:year/:month/:title/"

[params]
  # Main sections (currently used for the recent posts in the sidebar)
  mainSections = ["post"]

  # Default author
  author = "Amanda Regan"

  # Date format (default: Jan 2, 2006)
  # date_format = "Jan 2, 2006"

  # Display post summaries instead of content in list templates (default: true)
  truncate = true

  [params.sidebar]
    # Whether to show the recent posts or not (default: true)
    # showRecent = false

    # Whether to show the taxonomies or not (default: true)
    # showTaxonomy = false

    # How many posts to show on the sidebar (Default: 5)
    # num_recent_posts = 5

  [params.header]
    # Color scheme for the header (default: "light")
    # colorScheme = "dark"

  [params.footer]
    # Color scheme for the footer (default: "light")
    # colorScheme = "dark"
